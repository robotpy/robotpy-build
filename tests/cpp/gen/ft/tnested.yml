---

classes:
  Outer:
    shared_ptr: true
    template_params:
    - T
  
  Outer::Inner:
    shared_ptr: true
    attributes:
      t:

templates:
  TOuter:
    qualname: Outer
    params:
    - int
