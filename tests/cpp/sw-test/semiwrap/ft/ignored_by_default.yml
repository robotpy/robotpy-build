defaults:
  ignore: true
  # report_ignored_missing: false

enums:
  id_EnabledEnum:
    ignore: false
functions:
  id_fnEnable:
    ignore: false
classes:
  # id_IgnoreClass doesn't need to be specified because of default-ignore
  id_EnabledClass:
    ignore: false
    enums:
      InnerEnum:
    methods:
      fn: